The initial ad hoc full-text regular expression scan was stopped with SIGTERM (PID 62769, exit143) after taking too long; no benchmark overlapped it and raw histories were unchanged. The first streaming summarizer then missed singular `1 call` block headers. Its original script and slot-allocations.json remain as diagnostics, not accepted attribution. The corrected separate script recognizes both singular and plural headers and writes a new file. All byte counts are instrumented live-allocation sizes and require source/assembly correlation; they are not predictions of uninstrumented peak RSS.
