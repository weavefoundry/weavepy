"""``_pyrepl`` — package shell.

CPython's new REPL implementation. WeavePy carries only the
``_pyrepl.pager`` submodule (verbatim), which ``pydoc`` imports for its
paging helpers; the interactive REPL itself is Rust-native.
"""
