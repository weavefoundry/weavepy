# Ecosystem conformance baseline (RFC 0055 WS5; RFC 0062 WS3+WS4).
#
# One row per manifest package. Statuses:
#   pass — venv + install + probe all succeed
#   fail — known divergence; the run reports it as "expected fail" and a
#          flip to pass shows up as an improvement to ratchet
#   skip — do not run (e.g. needs network features we don't emulate yet)
#
# Per-OS overrides (RFC 0062 WS3): `status_macos` / `status_linux` /
# `status_windows` (and matching `reason_*`) replace `status`/`reason`
# on that host OS only. Unknown suffixes are a load error.
#
# Self-test tier (RFC 0062 WS4): rows whose manifest entry carries a
# `[packages.X.selftest]` table are also graded on `selftest_status`
# (default "pass"; same per-OS suffix scheme), reported independently
# of the probe status.
#
# `notes` is free-form context for humans. The runner exits non-zero on
# any status that differs from this file (unless --no-check).
#
# measured_os stamp (RFC 0063 WS7): the top-level list below names the
# OSes (std::env::consts::OS spelling, same as the suffix keys) whose
# rows are *measured* baselines. On a host OS not in the stamp, --check
# still prints the full report and writes results, but unexpected rows
# only emit an advisory NOTE and exit 0 — the gate stays advisory until
# a CI-measured Windows baseline is transplanted into status_windows
# rows and "windows" joins the stamp, flipping the lane to blocking. A
# missing stamp means "all OSes measured" (pre-RFC-0063 behaviour).

measured_os = ["macos", "linux"]

[packages.six]
status = "pass"

[packages.attrs]
status = "pass"
selftest_status = "pass"
selftest_reason = "re-measured 2026-08 on the wave-11 interpreter (RFC 0076 WS4): full suite, no hypothesis profile trim — 1379 passed / 0 failed / 6 skipped / 1 xfailed in 17:20, well inside the 2400s budget the wave-4 interpreter blew (killed at 2403s, tail unreached). Load-bearing engine fix before the clean run: zero-arg super() inside a method compiled by an enclosing function (attrs' generated slots-__getattr__ wrapper sets `__class__ = _cls` as a plain local) now resolves __class__ through normal lexical scoping — the local is promoted to a cell before emission. Canary: tests/regrtest/test_rfc0076_burn_regressions.py."

[packages.click]
status = "pass"

[packages.jinja2]
status = "pass"

[packages.requests]
status = "pass"
notes = "charset_normalizer is mypyc-compiled; exercised via the C-API tail."

[packages.dateutil]
status = "pass"

[packages.typing_extensions]
status = "pass"

[packages.packaging]
status = "pass"

[packages.pytest]
status = "pass"

# --- wave 2 (RFC 0056 WS5) ---------------------------------------------------

[packages.flask]
status = "pass"
notes = "route + test-client request/response, session cookie round-trip (werkzeug/itsdangerous/blinker in the chain)."

[packages.sqlalchemy]
status = "pass"
notes = "Core table create/insert/select + declarative ORM session over the RFC 0056 WS1 sqlite3; RFC 0066 WS4 adds the AsyncSession leg over aiosqlite (greenlet_spawn on the bundled native greenlet)."

[packages.httpx]
status = "pass"
notes = "sync + async GET against a local http.server (httpcore/anyio/sniffio/h11 chain)."

[packages.urllib3]
status = "pass"

[packages.pydantic]
status = "pass"
notes = "real pydantic-core wheel (PyO3/abi3 surface); validate/dump, field validators, ValidationError shape."

[packages.pyyaml]
status = "pass"
notes = "safe_load/safe_dump round-trip incl. anchors; pure-Python leg (wheel carries no _yaml here)."

[packages.msgpack]
status = "pass"

[packages.rich]
status = "pass"
notes = "console render to captured buffer, table + traceback formatting (markdown-it-py/pygments chain)."

[packages.tqdm]
status = "pass"

[packages.numpy]
status = "pass"
notes = "PyPI binary wheel (not source build), pinned 2.5.2 (RFC 0066 WS5): dtype arithmetic, broadcasting, linalg.norm, memoryview round-trip."
selftest_status = "fail"
selftest_reason = "re-measured 2026-08 (RFC 0076 WS1 burn, per-module): the RFC 0075 census (~74 fail + ~11 err across 21 modules) is burned — test_numeric (was 19), test_regression (7), test_conversion_utils (9), test_defchararray (6), test_array_coercion (5), test_nep50_promotions (4), test_mem_policy (9 err), test_multithreading, test_longdouble, and test_arrayprint are fully green; test_umath completes (was budget-bound) at 4680 passed / 2 failed in ~7 min and test_nditer at 930 passed / 1 failed. The six speed-bound modules now grade on the wave-11 interpreter: test_multiarray 14246 passed / 22 failed (1:18:32 — clusters: VM↔C Py_buffer re-export of non-contiguous shapes 11, ob_refcnt-visibility asserts 4, intp-converter error wording 2, fromfile-on-closed-fd error shape 2, singletons 3), test_strings 2011 passed / 0 residual (partition dispatch + _PyUnicode_IsTitlecase burned; canaries in test_rfc0076_burn_regressions.py), test_mem_overlap 25 passed / 0, test_scalarmath 1580 passed / 1, test_dtype 1176 passed / 3, test_stringdtype 2549 passed / 399 (one family: StringDType(coerce=False)'s C-side string check rejects values crossing the bridge — a follow-up burn target, not 399 root causes). Documented per-module deltas beyond those: exact-refcount-of-int asserts (unboxed immediates, architectural), test_out_wrap_no_leak (raw C-ref imbalance on the ndarray-subclass wrap path, tracked wave 7), np.add.__dict__ completeness, and one hypothesis too_slow health check. The graded single-invocation lane stays speed-bound regardless of test health: `--pyargs numpy._core` cannot fit the 2400s budget while test_multiarray alone runs 1:18 h — the stringdtype family + lane sharding are the wave-7 worklist."

[packages.markupsafe]
status = "pass"
notes = "compiled _speedups leg + escape correctness."

[packages.certifi]
status = "pass"

[packages.idna]
status = "pass"

[packages.charset_normalizer]
status = "pass"
notes = "mypyc-compiled wheel, exercised directly (previously only transitive via requests)."

# --- wave-2 stretch rows -----------------------------------------------------

[packages.cryptography]
status = "pass"
notes = "abi3 Rust wheel (_rust via cffi/PyO3): Fernet round-trip + X.509 parse."

[packages.orjson]
status = "pass"
notes = "PyO3 version-specific wheel: dumps/loads round-trip."

[packages.aiohttp]
status = "pass"
notes = "C-accelerated multidict/yarl/_http_parser chain over RFC 0054 asyncio; server+client round-trip."

# --- capstone (RFC 0056 WS6) ---------------------------------------------------

[packages.django]
status = "pass"
notes = "migrate + ORM CRUD + filter().count() + atomic() rollback + test-client request against the WS1 sqlite3 backend."

# --- capstone (RFC 0057 WS8) ---------------------------------------------------

[packages.pandas]
status = "pass"
notes = "full Cython _libs chain over the real numpy wheel: DataFrame ops, groupby/merge, DatetimeIndex (zoneinfo._zoneinfo), CSV round-trip, .str accessor."

[packages.fastapi]
status = "pass"
notes = "pydantic-core + starlette over anyio: routing, validation errors, DI, async endpoints, OpenAPI schema via TestClient."

# --- sdist proof rows (RFC 0062 WS2 via WS3) ----------------------------------

[packages.markupsafe_sdist]
status = "pass"
notes = "C sdist built through the local-index no_binary lane; probe asserts the compiled _speedups leg is live. Selftest measured 2026-08: 78/79 pass after the WS5 engine fixes (formatter_parser str-subclass, %(name)s non-dict mappings); the one deselect is enumerated in the manifest."

[packages.wrapt_sdist]
status = "pass"
notes = "C sdist built through the local-index no_binary lane; probe asserts the compiled _wrappers leg is live (the _PyReversed_Type / PyModule_GetDef / PyType_GetModule export gaps this row first surfaced are fixed in weavepy-capi)."

# --- heavy-native stack (RFC 0066 WS3) ----------------------------------------

[packages.scipy]
status = "pass"
notes = "measured 2026-08: linalg (LU/eig/f2py LAPACK), sparse CSR + spsolve, optimize (pybind11 _highspy HiGHS linprog + minimize), integrate.quad, stats.norm rvs/fit (PEP 604 union isinstance over ABC members), fft (pybind11 pocketfft), ndimage. First pybind11 rows: instancemethod type, TSS, interpreter-state identity, heap-type metaclass allocation, faithful PyCFunction m_self/m_ml, cyfunction __code__ as genuine types.CodeType."
selftest_status = "pass"
selftest_reason = "re-measured 2026-08 (RFC 0076 WS2 burn, installed lane scoped `--pyargs scipy.linalg scipy.fft`): 16954 passed / 0 failed / 448 skipped / 12 xfailed / 2 xpassed — a genuine zero, no deselects (even TestEig::test_singular, the Accelerate-LAPACK environmental flake the CPython 3.13.13 control fails, passes here). The RFC 0075 census's 46 deltas resolved to three root causes, none the predicted ones: interpolative's 40 were PyNumber_MatrixMultiply passing the bound receiver again (ndarray's 2-arg __matmul__ saw 3 args — now routed through the same nb_matrix_multiply/BinOpKind::MatMult dispatch as every operator), the 5 fft multiprocess-pickling failures were BUILTIN_WRITABLE_MODULE being thread-local (numpy's import-time `_reconstruct.__module__ = 'numpy._core.multiarray'` was invisible to the Pool task-feeder thread — now process-global), and Hadamard was math.log(4, 2) one ULP off (loghelper used _PyLong_Frexp for every int where CPython converts to double first). Wall 5:04 h under a concurrent full-sweep load; ~1:20 h quiet, inside the 14400s budget. Canaries in tests/regrtest/test_rfc0076_burn_regressions.py; scipy.sparse re-joining the scope stays the wave-7 question."

[packages.pillow]
status = "pass"
notes = "measured 2026-08: draw primitives, geometry ops, PNG/JPEG encoder round-trips through PyArg_ParseTuple y*/y#/z# and true-aliasing PyBytes_AsString, raw byte access, numpy interop, EXIF."
selftest_status = "pass"
selftest_reason = "re-measured 2026-08 (RFC 0076 WS3, sdist lane, 12.3.0 `Tests/` against the installed wheel): 4850 passed / 0 failed / 312 skipped / 3 xfailed in 40:30. All ten RFC 0075-era WeavePy-attributable deltas are burned down: the unclosed-file ResourceWarning quartet (POP_EXCEPT finalizable cascade), getextrema on I;16 (Py_BuildValue 'HH'), the getim PyCapsule type, the ImageCms PyNumber_Index TypeError surface, ImageFontPIL's strict Latin-1 UnicodeEncodeError, and the default-font leak ceiling (byte-accounted scalar-pin + AsUTF8 C-string cache sweeps — the caches previously pinned each marshaled ~10 KB text copy forever, ~60 KB/draw.text call). The extra ~185 skips vs CPython are feature-detection (deferred codecs/plugins), enumerated in the lane log, not verdict-bearing. test_tiff_crashes is deselected in the manifest: its crash corpus is repo-only (not in the sdist) and upstream re-raises the FileNotFoundError only under CI — environmental on GitHub Actions, skips locally, no WeavePy code exercised."

[packages.lxml]
status = "pass"
notes = "measured 2026-08: etree round-trip, namespaced XPath, XSLT (spec-struct protocol slots incl. tp_descr_get), iterparse, lxml.html rewrite, XMLSchema validation, deepcopy/pickle."
selftest_status = "fail"
selftest_reason = "re-measured 2026-08 (RFC 0076 WS3 burn, overlay lane over the 6.1.2 sdist tests): 368 failed / 1892 passed / 54 skipped in 1:07:32 — the CPython 3.13.13 control over the identical overlay measures 368 failed / 1888 passed, so the WeavePy-attributable delta is 0 (was 44). The lane stays intrinsically red under `pytest --pyargs lxml.tests` on both interpreters: pytest collects the module-level abstract unittest bases (`_ETreeTestCaseBase` &co. run with `etree = None`) and the doctest-loading `test_suite()` functions FileNotFoundError outside the sdist root — upstream collection artifacts, this row's grading basis. The 44-delta burn's load-bearing fixes: Cython 3-arg raise (traceback/frame type identity), dunder-shim precedence for tp_methods already in tp_dict, faithful-list mirror reconciliation for never-registered mirrors (lxml.sax `_element_stack`), `note_c_agreement` Rc fingerprinting (descendantpaths accumulation), PySlice_Unpack overflow clamping, pyexpat y# parse args, `__delattr__` from tp_setattro, dir() through a C type's __dict__ property override, and ctypes c_char_p interned-buffer marshalling (adopt_external_document strcmp). Canaries: tests/regrtest/test_rfc0076_burn_regressions.py."

[packages.greenlet]
status = "pass"
notes = "measured 2026-08 (RFC 0066 WS4): the bundled native greenlet over real stack switching — producer/consumer ping-pong, exception + GreenletExit plumbing, gr_frame, contextvars isolation, deep recursion on the dedicated stack, dist-info visibility."

[packages.matplotlib]
status = "pass"
notes = "measured 2026-08 (RFC 0066 WS6 stretch): Agg figure render + PNG round-trip + savefig buffer. Landed on the way: CPython-faithful abstract-instantiation gate (native allocator bypasses __abstractmethods__), PyType_Type.tp_call for pybind11_meta_call, __class__ cell for methods that locally bind super, foreign tp_call fast path (pybind11 instancemethod repr during m.def default-arg registration), LoadAttrType inline-cache deopt for foreign descriptors (FT2Font.get_sfnt lost self on the second dispatch), math index coercion driving nb_index on foreign numpy scalars (bezier _comb via np.vectorize)."

[packages.gevent]
status = "pass"
notes = "measured 2026-08 (RFC 0072 WS2): monkey.patch_all + spawn fan-out/joinall + gevent.sleep cooperative ordering + loopback echo over the patched socket module, riding the native greenlet through the greenlet._C_API capsule. Landed on the way: import-time-bound _thread.get_ident in _threading_local (patch_all swaps _thread.get_ident for a greenlet-id version, which silently flipped every pre-patch threading.local — including gevent's own hub registry — to greenlet-local storage), sys.exc_info() fallback to the C-API handled-exception slot (Cython except blocks park the caught exception where only PyErr_GetHandledException sees it), _socket EAI_* constants (gevent.resolver imports EAI_NONAME/EAI_SERVICE at module scope), cross-thread greenlet start (the registry is process-global and a greenlet binds to a thread at first switch, which gevent's threadpool worker pattern requires), type(lock).acquire unbound-method trampolines, and PyMemoryView_Type bridged to the real VM memoryview (Cython caches _memoryview = memoryview as the static and calls it)."

# --- ecosystem wave 5 (RFC 0075 WS9) -------------------------------------------

[packages.scikit_learn]
status = "pass"
notes = "measured 2026-08 (RFC 0075 WS9): LogisticRegression fit/predict/score, RandomForestClassifier n_jobs=2 (real loky process pool — spawn, memmap arena, SemLock keyword construction), StandardScaler+SVC pipeline under GridSearchCV (2×2), joblib.Memory cache round-trip. Landed on the way: _multiprocessing.SemLock keyword args (joblib constructs SemLock(0, 0, 1, name=…, unlink=True) by keyword), faithful tp_basicsize for pure-Python mirrors of C builtins (array.array reports CPython's 64 — sklearn's Cython utils size-check it at import)."

[packages.gunicorn_gevent]
status = "pass"
notes = "measured 2026-08 (RFC 0075 WS9 capstone): gunicorn --workers 2 -k gevent serving a Django WSGI app — readiness-polled startup, concurrent requests through the patched-socket path answered by both workers, SIGTERM graceful shutdown with exit code 0. Landed on the way: pwd/grp built-in modules (gunicorn imports both at module level for its user-switching surface), os.utime on a file descriptor (futimens — the worker heartbeat is os.utime(tmp.fileno(), (t, t)) and every worker died at first notify without it)."

# --- ecosystem wave 6 (RFC 0076 WS5) -------------------------------------------

[packages.polars]
status = "pass"

[packages.psycopg2]
status = "pass"

[packages.celery]
status = "pass"

[packages.uvicorn]
status = "pass"

[packages.alembic]
status = "pass"

[packages.torch]
status = "pass"

# --- infrastructure-native tail (RFC 0072 WS3/WS4) -----------------------------

[packages.uvloop]
status = "pass"
status_windows = "skip"
reason_windows = "no upstream Windows support — uvloop publishes no Windows wheels (libuv loop is POSIX-only there by design)."
notes = "measured 2026-08 (RFC 0072 WS3): policy install, gather fan-out, loopback TCP echo through uvloop transports, UDP datagram endpoint round-trip, loop.getaddrinfo, run_in_executor, subprocess_exec. Landed on the way: PyImport_ImportModuleLevelObject returns the root package for empty-fromlist dotted imports (Cython's __Pyx_ImportDottedModule binds `asyncio`, not the leaf), PyContext_CopyCurrent/Enter/Exit bridged to contextvars (uvloop runs every callback under a copied context), PyOS_setsig/PyThread_get_thread_ident/PyDescr_IsData and the fork hooks, a faithful C-visible exception tail on identity boxes (PyBaseExceptionObject fields at ABI offsets; Cython fetches StopIteration.value with a direct struct read), value-preserving tp_iternext bridges + PyIter_Send (the swallowing PyIter_Next lane made every compiled await of a VM awaitable complete with None), and PySet_Discard's found/not-found return contract (Cython's set.remove raised KeyError for every uvloop timer cancellation)."

[packages.psycopg]
status = "pass"
notes = "measured 2026-08 (RFC 0072 WS4): serverless but behavior-asserting — pq.__impl__ == 'binary' (the Cython/libpq wheel is the live implementation), SQL composition, Transformer dumper/loader round-trips, conninfo parse/build, and a real libpq connect driven to OperationalError against a closed loopback port. Landed on the way: PyOS_double_to_string (psycopg's FloatDumper renders every float parameter through it; the missing symbol was a NULL jump). One cosmetic divergence: the failed-connect cleanup prints an ignored finalizer AttributeError to stderr that CPython formats as 'Exception ignored in'."

[packages.grpcio]
status = "pass"
notes = "measured 2026-08 (RFC 0075 WS7 burn): in-process server + GenericRpcHandler bytes-identity unary echo round-trips clean. The RFC 0072 red row's \"empty traceback in the _serve poller thread\" was not thread-state at all: cygrpc's poller slices a `bytes` completion tag through `PySequence_GetSlice`, whose list/tuple/str-only table raised a TypeError that Cython's nogil-adjacent except path swallowed to an empty traceback. `PySequence_GetSlice` now routes every object through the VM subscript dispatch (bytes/bytearray/memoryview, instance `__getitem__`, extension slot wrappers), with `_PyEval_SliceIndex`/`_PyEval_SliceIndexNotNone` landed alongside."
