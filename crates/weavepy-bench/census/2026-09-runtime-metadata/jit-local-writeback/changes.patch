--- a/crates/weavepy-jit/src/engine.rs
+++ b/crates/weavepy-jit/src/engine.rs
@@ -127,9 +127,13 @@
     /// # Safety
     ///
     /// `frame` must point to a fully-initialised [`JitFrame`] whose
-    /// `locals` / `stack_spill` / `stack_tags` buffers are at least
-    /// `n_locals` / `max_stack` wide, and the owning [`JitEngine`] must
-    /// still be alive (its `JITModule` backs this function pointer).
+    /// `locals` and `local_written` buffers each contain at least `n_locals`
+    /// initialized entries, and `stack_spill` and `stack_tags` each contain
+    /// at least `max_stack` entries. These writable buffers must be disjoint
+    /// and remain live throughout entry. Assignment flags start at zero for
+    /// a new activation and persist across native generator resumes. The
+    /// owning [`JitEngine`] must still be alive (its `JITModule` backs this
+    /// function pointer).
     #[must_use]
     pub unsafe fn enter(&self, frame: *mut JitFrame) -> JitStatus {
         // SAFETY: the caller upholds the buffer-size and liveness
--- a/crates/weavepy-jit/src/engine/tests.rs
+++ b/crates/weavepy-jit/src/engine/tests.rs
@@ -106,8 +106,10 @@
         let mut locals = [a as u64, b as u64];
         let mut spill = [0u64; 3];
         let mut tags = [0u32; 3];
+        let mut local_written = vec![0u32; locals.len()];
         let mut frame = JitFrame {
             locals: locals.as_mut_ptr(),
+            local_written: local_written.as_mut_ptr(),
             n_locals: 2,
             entry_pc: 0,
             ret_bits: 0,
@@ -152,8 +154,10 @@
     let mut locals = [41u64, 7];
     let mut spill = [0u64; 3];
     let mut tags = [0u32; 3];
+    let mut local_written = vec![0u32; locals.len()];
     let mut frame = JitFrame {
         locals: locals.as_mut_ptr(),
+        local_written: local_written.as_mut_ptr(),
         n_locals: 2,
         entry_pc: 0,
         ret_bits: 0,
@@ -194,8 +198,10 @@
     let mut locals = [a as u64, b as u64];
     let mut spill = [0u64; 3];
     let mut tags = [0u32; 3];
+    let mut local_written = vec![0u32; locals.len()];
     let mut frame = JitFrame {
         locals: locals.as_mut_ptr(),
+        local_written: local_written.as_mut_ptr(),
         n_locals: 2,
         entry_pc: 0,
         ret_bits: 0,
@@ -292,8 +298,10 @@
     assert_eq!(locals.len(), compiled.n_locals as usize);
     assert!(spill.len() >= compiled.max_stack as usize);
     assert!(tags.len() >= spill.len());
+    let mut local_written = vec![0u32; locals.len()];
     let mut frame = JitFrame {
         locals: locals.as_mut_ptr(),
+        local_written: local_written.as_mut_ptr(),
         n_locals: compiled.n_locals,
         entry_pc,
         ret_bits: 0,
@@ -311,6 +319,7 @@
     // operations and explicit exits. Opaque pin bits are copied, never read
     // through a helper. All buffers fit the compiled metadata and stay alive.
     assert_eq!(unsafe { compiled.enter(&raw mut frame) }, expected);
+    frame.local_written = std::ptr::null_mut();
     frame
 }
 
@@ -530,3 +539,184 @@
         assert_eq!(frame.ret_tag, SlotTag::Int as u32);
     }
 }
+
+fn run_local_exit_probe_flags(
+    compiled: &super::CompiledFrame,
+    entry_pc: u32,
+    locals: &mut [u64],
+    spill: &mut [u64],
+    tags: &mut [u32],
+    local_written: &mut [u32],
+    expected: crate::runtime::JitStatus,
+) -> crate::runtime::JitFrame {
+    use crate::runtime::JitFrame;
+    assert_eq!(locals.len(), compiled.n_locals as usize);
+    assert!(spill.len() >= compiled.max_stack as usize);
+    assert!(tags.len() >= spill.len());
+    assert_eq!(local_written.len(), locals.len());
+    let mut frame = JitFrame {
+        locals: locals.as_mut_ptr(),
+        local_written: local_written.as_mut_ptr(),
+        n_locals: compiled.n_locals,
+        entry_pc,
+        ret_bits: 0,
+        ret_tag: 0,
+        deopt_pc: 0,
+        stack_spill: spill.as_mut_ptr(),
+        stack_tags: tags.as_mut_ptr(),
+        stack_len: 0,
+        stack_cap: spill.len() as u32,
+        ctx: std::ptr::null_mut(),
+        call_args: std::ptr::null_mut(),
+        call_tags: std::ptr::null_mut(),
+    };
+    // SAFETY: these tests retain the owning engine and use only local scalar
+    // operations and explicit exits. Opaque pin bits are copied, never read
+    // through a helper. All buffers fit the compiled metadata and stay alive.
+    assert_eq!(unsafe { compiled.enter(&raw mut frame) }, expected);
+    frame
+}
+
+#[test]
+fn assignment_flags_follow_conditional_stores_at_a_shared_exit() {
+    use crate::runtime::JitStatus;
+    for lane in [JitType::Int, JitType::Float, JitType::Bool, JitType::Obj] {
+        let mut tf = function(&[TOp::LoadLocal(0)]);
+        tf.n_locals = 3;
+        tf.local_types = vec![Some(JitType::Int), Some(lane), Some(lane)];
+        tf.livein_locals = vec![0, 2];
+        tf.blocks[0].term = TTerm::BranchFalse {
+            target: 2,
+            fallthrough: 1,
+        };
+        let mut assigned = function(&[TOp::LoadLocal(2), TOp::StoreLocal(1)])
+            .blocks
+            .remove(0);
+        assigned.term = TTerm::Jump(3);
+        let mut skipped = function(&[]).blocks.remove(0);
+        skipped.term = TTerm::Jump(3);
+        let mut exit = function(&[]).blocks.remove(0);
+        exit.term = TTerm::Deopt { pc: 31 };
+        tf.blocks.extend([assigned, skipped, exit]);
+        let mut engine = super::JitEngine::new().unwrap();
+        let cf = engine.compile_tfunc(&tf).unwrap();
+        for condition in [0, 1] {
+            let mut locals = [condition, 99, 1];
+            let mut written = [0; 3];
+            let mut spill = [0; 2];
+            let mut tags = [0; 2];
+            let frame = run_local_exit_probe_flags(
+                &cf,
+                0,
+                &mut locals,
+                &mut spill,
+                &mut tags,
+                &mut written,
+                JitStatus::Deopt,
+            );
+            assert_eq!(frame.deopt_pc, 31);
+            assert_eq!(written, [0, condition as u32, 0]);
+            assert_eq!(locals[1], if condition == 0 { 99 } else { 1 });
+        }
+    }
+}
+
+#[test]
+fn assignment_flags_distinguish_empty_loops_and_osr_assignments() {
+    use crate::ir::OsrEntry;
+    use crate::runtime::JitStatus;
+    let mut tf = function(&[]);
+    tf.n_locals = 3;
+    tf.local_types = vec![Some(JitType::Int); 3];
+    tf.livein_locals = vec![0, 1];
+    tf.blocks[0].term = TTerm::Jump(1);
+    let mut header = function(&[]).blocks.remove(0);
+    header.term = TTerm::ForRange {
+        cur_slot: 0,
+        stop_slot: 1,
+        var_slot: 2,
+        body: 2,
+        exit: 3,
+    };
+    let mut body = function(&[]).blocks.remove(0);
+    body.term = TTerm::Jump(1);
+    let mut exit = function(&[]).blocks.remove(0);
+    exit.term = TTerm::Deopt { pc: 80 };
+    tf.blocks.extend([header, body, exit]);
+    tf.osr_entries.push(OsrEntry {
+        pc: 42,
+        block: 1,
+        unassigned_reads: vec![0, 1],
+    });
+    let mut engine = super::JitEngine::new().unwrap();
+    let cf = engine.compile_tfunc(&tf).unwrap();
+    for entry in [0, 42] {
+        for (start, assigned, final_target) in [(1, 1, 2), (3, 0, 99)] {
+            let mut locals = [start, 3, 99];
+            let mut written = [0; 3];
+            let mut spill = [0; 2];
+            let mut tags = [0; 2];
+            let frame = run_local_exit_probe_flags(
+                &cf,
+                entry,
+                &mut locals,
+                &mut spill,
+                &mut tags,
+                &mut written,
+                JitStatus::Deopt,
+            );
+            assert_eq!(frame.deopt_pc, 80);
+            assert_eq!(locals, [3, 3, final_target]);
+            assert_eq!(written, [assigned, 0, assigned]);
+        }
+    }
+}
+
+#[test]
+fn assignment_flags_survive_a_native_generator_resume() {
+    use crate::ir::OsrEntry;
+    use crate::runtime::JitStatus;
+    let mut tf = function(&[TOp::PushConstInt(7), TOp::StoreLocal(1), TOp::LoadLocal(0)]);
+    tf.local_types[0] = Some(JitType::Obj);
+    tf.livein_locals = vec![0];
+    tf.blocks[0].term = TTerm::Yield { pc: 10 };
+    let mut resume = function(&[TOp::Pop]).blocks.remove(0);
+    resume.entry_stack = vec![JitType::Obj];
+    resume.term = TTerm::Deopt { pc: 32 };
+    tf.blocks.push(resume);
+    tf.resume_entries.push(OsrEntry {
+        pc: 11,
+        block: 1,
+        unassigned_reads: vec![],
+    });
+    let mut engine = super::JitEngine::new().unwrap();
+    let cf = engine.compile_tfunc(&tf).unwrap();
+    let mut locals = [u64::MAX, 99];
+    let mut written = [0; 2];
+    let mut spill = [0; 2];
+    let mut tags = [0; 2];
+    let first = run_local_exit_probe_flags(
+        &cf,
+        0,
+        &mut locals,
+        &mut spill,
+        &mut tags,
+        &mut written,
+        JitStatus::Yielded,
+    );
+    assert_eq!(first.deopt_pc, 10);
+    assert_eq!(locals[1], 7);
+    assert_eq!(written, [0, 1]);
+    let last = run_local_exit_probe_flags(
+        &cf,
+        11,
+        &mut locals,
+        &mut spill,
+        &mut tags,
+        &mut written,
+        JitStatus::Deopt,
+    );
+    assert_eq!(last.deopt_pc, 32);
+    assert_eq!(locals[1], 7);
+    assert_eq!(written, [0, 1]);
+}
--- a/crates/weavepy-jit/src/lower.rs
+++ b/crates/weavepy-jit/src/lower.rs
@@ -23,6 +23,7 @@
 use crate::value::JitType;
 
 const OFF_LOCALS: i32 = core::mem::offset_of!(JitFrame, locals) as i32;
+const OFF_LOCAL_WRITTEN: i32 = core::mem::offset_of!(JitFrame, local_written) as i32;
 const OFF_ENTRY_PC: i32 = core::mem::offset_of!(JitFrame, entry_pc) as i32;
 const OFF_RET_BITS: i32 = core::mem::offset_of!(JitFrame, ret_bits) as i32;
 const OFF_RET_TAG: i32 = core::mem::offset_of!(JitFrame, ret_tag) as i32;
@@ -62,6 +63,8 @@
     cl_blocks: Vec<Block>,
     /// One variable per managed local slot (others unused).
     vars: Vec<Option<Variable>>,
+    written: Vec<Option<Variable>>,
+    written_base: Value,
     frame_ptr: Value,
     locals_base: Value,
     spill_base: Value,
@@ -123,6 +126,8 @@
             ptr_ty,
             cl_blocks: Vec::new(),
             vars: Vec::new(),
+            written: Vec::new(),
+            written_base: dummy,
             frame_ptr: dummy,
             locals_base: dummy,
             spill_base: dummy,
@@ -179,6 +184,10 @@
             .b
             .ins()
             .load(self.ptr_ty, trusted, self.frame_ptr, OFF_LOCALS);
+        self.written_base =
+            self.b
+                .ins()
+                .load(self.ptr_ty, trusted, self.frame_ptr, OFF_LOCAL_WRITTEN);
         self.spill_base = self
             .b
             .ins()
@@ -253,6 +262,7 @@
 
         // Declare + initialise a variable per managed local.
         self.vars = vec![None; self.tfunc.n_locals as usize];
+        self.written = vec![None; self.tfunc.n_locals as usize];
         for slot in 0..self.tfunc.local_types.len() {
             if let Some(ty) = self.tfunc.local_types[slot] {
                 let cl = Self::cl_ty(ty);
@@ -261,6 +271,19 @@
                 let v = self.b.ins().load(cl, trusted, self.locals_base, off);
                 self.b.def_var(var, v);
                 self.vars[slot] = Some(var);
+                let written = self.b.declare_var(types::I32);
+                // Fresh entries, including OSR, preserve the interpreter's
+                // original objects until assignment. Parked generators keep
+                // assignments made before earlier yields in their buffers.
+                let seed = if self.tfunc.resume_entries.is_empty() {
+                    self.b.ins().iconst(types::I32, 0)
+                } else {
+                    self.b
+                        .ins()
+                        .load(types::I32, trusted, self.written_base, (slot as i32) * 4)
+                };
+                self.b.def_var(written, seed);
+                self.written[slot] = Some(written);
             }
         }
 
@@ -413,8 +436,10 @@
                 self.b.ins().brif(cond, body_pre, &[], eb, &[]);
                 self.b.switch_to_block(body_pre);
                 self.b.def_var(loop_var, cur);
+                self.mark_written(var_slot);
                 let next = self.b.ins().iadd_imm(cur, 1);
                 self.b.def_var(cur_var, next);
+                self.mark_written(cur_slot);
                 let bb = self.cl_blocks[body];
                 self.b.ins().jump(bb, &[]);
             }
@@ -466,8 +491,10 @@
                         .ins()
                         .load(Self::cl_ty(elem), trusted, self.frame_ptr, OFF_RET_BITS);
                 self.b.def_var(loop_var, res);
+                self.mark_written(var_slot);
                 let next = self.b.ins().iadd_imm(idx, 1);
                 self.b.def_var(idx_var, next);
+                self.mark_written(idx_slot);
                 let bb = self.cl_blocks[body];
                 self.b.ins().jump(bb, &args);
                 self.b.switch_to_block(rest_b);
@@ -536,6 +563,7 @@
                         .ins()
                         .load(Self::cl_ty(elem), trusted, self.frame_ptr, OFF_RET_BITS);
                 self.b.def_var(loop_var, res);
+                self.mark_written(var_slot);
                 let bb = self.cl_blocks[body];
                 self.b.ins().jump(bb, &args);
                 self.b.switch_to_block(rest_b);
@@ -624,11 +652,13 @@
                         .ins()
                         .load(Self::cl_ty(elem1), trusted, self.frame_ptr, OFF_RET_BITS);
                 self.b.def_var(var1, e1);
+                self.mark_written(var1_slot);
                 let e2 = self
                     .b
                     .ins()
                     .load(Self::cl_ty(elem2), trusted, self.call_args_base, 0);
                 self.b.def_var(var2, e2);
+                self.mark_written(var2_slot);
                 let bb = self.cl_blocks[body];
                 self.b.ins().jump(bb, &args);
                 self.b.switch_to_block(rest_b);
@@ -730,6 +760,7 @@
                 let (v, _) = self.pop();
                 let var = self.vars[slot as usize].expect("managed local");
                 self.b.def_var(var, v);
+                self.mark_written(slot);
             }
             TOp::IntArith(kind) => self.emit_int_arith(kind, stmt.pc),
             TOp::FloatArith(kind) => self.emit_float_arith(kind, stmt.pc),
@@ -950,6 +981,7 @@
         self.b.switch_to_block(cont);
         let iter_var = self.vars[iter_slot as usize].expect("managed iter slot");
         self.b.def_var(iter_var, res);
+        self.mark_written(iter_slot);
     }
 
     /// RFC 0074 WS2 — the opaque-call lane via `wpjit_call_dyn`:
@@ -1446,6 +1478,7 @@
         self.b.switch_to_block(cont);
         let iter_var = self.vars[iter_slot as usize].expect("managed iter slot");
         self.b.def_var(iter_var, pin);
+        self.mark_written(iter_slot);
     }
 
     /// RFC 0073 WS2 — the dict-loop capture behind an erased
@@ -1472,6 +1505,7 @@
         self.b.switch_to_block(cont);
         let iter_var = self.vars[iter_slot as usize].expect("managed iter slot");
         self.b.def_var(iter_var, res);
+        self.mark_written(iter_slot);
     }
 
     /// RFC 0071 WS4 — `BUILD_LIST k`: the elements are staged through
@@ -3120,7 +3154,13 @@
         self.emit_exit(pc, snapshot, JitStatus::Deopt);
     }
 
-    /// Write back every managed local into the frame's locals buffer.
+    fn mark_written(&mut self, slot: u32) {
+        let var = self.written[slot as usize].expect("managed assignment flag");
+        let one = self.b.ins().iconst(types::I32, 1);
+        self.b.def_var(var, one);
+    }
+
+    /// Write back values and path-specific assignment flags together.
     fn writeback_locals(&mut self) {
         let trusted = MemFlags::trusted();
         for slot in 0..self.vars.len() {
@@ -3128,6 +3168,12 @@
                 let v = self.b.use_var(var);
                 let off = (slot as i32) * 8;
                 self.b.ins().store(trusted, v, self.locals_base, off);
+                let written = self
+                    .b
+                    .use_var(self.written[slot].expect("managed assignment flag"));
+                self.b
+                    .ins()
+                    .store(trusted, written, self.written_base, (slot as i32) * 4);
             }
         }
     }
--- a/crates/weavepy-jit/src/runtime.rs
+++ b/crates/weavepy-jit/src/runtime.rs
@@ -153,6 +153,11 @@
     /// Slot-indexed local storage, one `u64` per code-object local.
     /// Holds `i64` / `f64`-bits / `bool` per the local's stable type.
     pub locals: *mut u64,
+    /// One assignment flag per local, in disjoint `n_locals`-wide storage.
+    /// A nonzero flag means native code assigned this slot. On side exits,
+    /// unassigned slots retain their original interpreter objects. Initialize
+    /// flags to zero for a new activation and preserve them across yields.
+    pub local_written: *mut u32,
     /// Number of valid entries in [`Self::locals`].
     pub n_locals: u32,
     /// OSR entry: the bytecode pc to begin execution at. `0` enters at
--- a/crates/weavepy-jit/tests/numeric.rs
+++ b/crates/weavepy-jit/tests/numeric.rs
@@ -22,8 +22,10 @@
     let mut spill = vec![0u64; cap];
     let mut tags = vec![0u32; cap];
 
+    let mut local_written = vec![0u32; locals.len()];
     let mut frame = JitFrame {
         locals: locals.as_mut_ptr(),
+        local_written: local_written.as_mut_ptr(),
         n_locals: cf.n_locals,
         entry_pc: 0,
         ret_bits: 0,
--- a/crates/weavepy-vm/src/tier2.rs
+++ b/crates/weavepy-vm/src/tier2.rs
@@ -2842,6 +2842,7 @@
 #[inline(never)]
 unsafe fn enter_scalar_leaf(nc: &NativeCallee, jf: &JitFrame, argc: usize) -> Option<(u64, u32)> {
     let mut locals = [0u64; SCALAR_LEAF_SLOTS];
+    let mut local_written = [0u32; SCALAR_LEAF_SLOTS];
     for (j, slot) in locals.iter_mut().enumerate().take(argc) {
         // SAFETY: the caller validated the marshaled argument prefix.
         *slot = unsafe { *jf.call_args.add(j) };
@@ -2859,6 +2860,7 @@
     let mut tags = [0u32; SCALAR_LEAF_SLOTS];
     let mut frame = JitFrame {
         locals: locals.as_mut_ptr(),
+        local_written: local_written.as_mut_ptr(),
         n_locals: nc.cf.n_locals,
         entry_pc: 0,
         ret_bits: 0,
@@ -3057,8 +3059,9 @@
     let mut u64_buf = take_u64(n_locals + cap + call_cap);
     let (locals_buf, rest) = u64_buf.split_at_mut(n_locals);
     let (spill, call_args) = rest.split_at_mut(cap);
-    let mut u32_buf = take_u32(cap + call_cap);
-    let (tags, call_tags) = u32_buf.split_at_mut(cap);
+    let mut u32_buf = take_u32(n_locals + cap + call_cap);
+    let (local_written, rest) = u32_buf.split_at_mut(n_locals);
+    let (tags, call_tags) = rest.split_at_mut(cap);
     let mut pins: PinTable = Vec::new();
     if let Some(r) = recv {
         // The receiver slot carries pin index 0 (`take_u64` zeroed it).
@@ -3160,6 +3163,7 @@
     };
     let mut njf = JitFrame {
         locals: locals_buf.as_mut_ptr(),
+        local_written: local_written.as_mut_ptr(),
         n_locals: nc.cf.n_locals,
         entry_pc: 0,
         ret_bits: 0,
@@ -3226,7 +3230,15 @@
                 None
             };
             match finish_deopted_callee(
-                interp, nc, &mut nctx, locals_buf, spill, tags, &njf, pending,
+                interp,
+                nc,
+                &mut nctx,
+                locals_buf,
+                local_written,
+                spill,
+                tags,
+                &njf,
+                pending,
             ) {
                 Ok(v) => Done::Obj(v),
                 Err(e) => Done::Raised(e),
@@ -3416,6 +3428,7 @@
     nc: &NativeCallee,
     nctx: &mut CallCtx,
     locals_buf: &[u64],
+    local_written: &[u32],
     spill: &[u64],
     tags: &[u32],
     njf: &JitFrame,
@@ -3424,7 +3437,13 @@
     let code = &nc.code;
     let n_real = code.varnames.len();
     let mut locals_v: Vec<Object> = Vec::with_capacity(n_real);
-    for slot in 0..n_real {
+    for (slot, &written) in local_written.iter().enumerate().take(n_real) {
+        // Native entry checked every parameter lane. Other slots are unbound
+        // until their assignment actually executes on this path.
+        if slot >= code.arg_count as usize && written == 0 {
+            locals_v.push(Object::Unbound);
+            continue;
+        }
         match nc.cf.local_types.get(slot).copied().flatten() {
             Some(ty) => locals_v.push(unpack_ty(
                 locals_buf.get(slot).copied().unwrap_or(0),
@@ -6857,8 +6876,9 @@
     let mut u64_buf = take_u64(n + cap + call_cap);
     let (locals_buf, rest) = u64_buf.split_at_mut(n);
     let (spill, call_args) = rest.split_at_mut(cap);
-    let mut u32_buf = take_u32(cap + call_cap);
-    let (tags, call_tags) = u32_buf.split_at_mut(cap);
+    let mut u32_buf = take_u32(n + cap + call_cap);
+    let (local_written, rest) = u32_buf.split_at_mut(n);
+    let (tags, call_tags) = rest.split_at_mut(cap);
     let mut pins: PinTable = Vec::new();
     if method_shape {
         // The receiver slot carries pin index 0 (`take_u64` zeroed it).
@@ -6912,6 +6932,7 @@
     };
     let mut jf = JitFrame {
         locals: locals_buf.as_mut_ptr(),
+        local_written: local_written.as_mut_ptr(),
         n_locals: cf.n_locals,
         entry_pc: 0,
         ret_bits: 0,
@@ -6981,7 +7002,17 @@
                 code: code.clone(),
                 ctor: None,
             };
-            finish_deopted_callee(interp, &nc, &mut ctx, locals_buf, spill, tags, &jf, pending)
+            finish_deopted_callee(
+                interp,
+                &nc,
+                &mut ctx,
+                locals_buf,
+                local_written,
+                spill,
+                tags,
+                &jf,
+                pending,
+            )
         }
     };
     // Reap the activation's pins (after the
@@ -7465,6 +7496,7 @@
     let cf = &entry.cf;
     let n = cf.n_locals as usize;
     let mut locals_buf = take_u64(n);
+    let mut local_written = take_u32(n);
     // RFC 0061/0065 WS5 — pin every list-/instance-lane local: the
     // slot carries an index into `pins`, and the table (not the slot)
     // keeps the object alive and reachable for the access helpers and
@@ -7592,6 +7624,7 @@
     };
     let mut jf = JitFrame {
         locals: locals_buf.as_mut_ptr(),
+        local_written: local_written.as_mut_ptr(),
         n_locals: cf.n_locals,
         entry_pc,
         ret_bits: resume_bits.unwrap_or(0),
@@ -7629,6 +7662,7 @@
                 compile_id: entry.compile_id,
                 yield_pc: jf.deopt_pc,
                 locals_buf,
+                local_written,
                 spill,
                 tags,
                 call_args,
@@ -7659,6 +7693,7 @@
             frame,
             entry,
             &locals_buf,
+            &local_written,
             &spill,
             &tags,
             &jf,
@@ -7671,6 +7706,7 @@
     // above, so nothing is unpacked from a drained table).
     drain_activation_pins(interp, &mut ctx.pins);
     put_u64(locals_buf);
+    put_u32(local_written);
     put_u64(spill);
     put_u32(tags);
     put_u64(call_args);
@@ -7762,6 +7798,7 @@
     frame: &mut super::Frame,
     entry: &CompiledEntry,
     locals_buf: &[u64],
+    local_written: &[u32],
     spill: &[u64],
     tags: &[u32],
     jf: &JitFrame,
@@ -7772,6 +7809,9 @@
     {
         let mut locals = frame.locals.borrow_mut();
         for (slot, &bits) in locals_buf.iter().enumerate() {
+            if local_written[slot] == 0 {
+                continue;
+            }
             if let Some(ty) = cf.local_types[slot] {
                 if let Some(dst) = locals.get_mut(slot) {
                     *dst = unpack_ty(bits, ty, &ctx.pins);
@@ -8082,6 +8122,7 @@
     /// `frame.pc` carries the continuation).
     yield_pc: u32,
     locals_buf: Vec<u64>,
+    local_written: Vec<u32>,
     spill: Vec<u64>,
     tags: Vec<u32>,
     call_args: Vec<u64>,
@@ -8293,6 +8334,9 @@
     {
         let mut locals = frame.locals.borrow_mut();
         for (slot, &bits) in act.locals_buf.iter().enumerate() {
+            if act.local_written[slot] == 0 {
+                continue;
+            }
             if let Some(ty) = act.local_types.get(slot).copied().flatten() {
                 if let Some(dst) = locals.get_mut(slot) {
                     *dst = unpack_ty(bits, ty, &act.pins);
@@ -8340,6 +8384,7 @@
     // Buffers return to the pools.
     let NativeActivation {
         locals_buf,
+        local_written,
         spill,
         tags,
         call_args,
@@ -8347,6 +8392,7 @@
         ..
     } = *act;
     put_u64(locals_buf);
+    put_u32(local_written);
     put_u64(spill);
     put_u32(tags);
     put_u64(call_args);
@@ -8451,6 +8497,7 @@
     };
     let mut jf = JitFrame {
         locals: act.locals_buf.as_mut_ptr(),
+        local_written: act.local_written.as_mut_ptr(),
         n_locals: cf.n_locals,
         entry_pc: pc,
         ret_bits: resume_bits,
@@ -8497,6 +8544,9 @@
             // real last values, not the park-time snapshot.
             let mut locals = frame.locals.borrow_mut();
             for (slot, &bits) in act.locals_buf.iter().enumerate() {
+                if act.local_written[slot] == 0 {
+                    continue;
+                }
                 if let Some(ty) = act.local_types.get(slot).copied().flatten() {
                     if let Some(dst) = locals.get_mut(slot) {
                         *dst = unpack_ty(bits, ty, &ctx.pins);
@@ -8511,6 +8561,7 @@
             frame,
             &entry,
             &act.locals_buf,
+            &act.local_written,
             &act.spill,
             &act.tags,
             &jf,
@@ -8521,6 +8572,7 @@
     drain_activation_pins(interp, &mut ctx.pins);
     let NativeActivation {
         locals_buf,
+        local_written,
         spill,
         tags,
         call_args,
@@ -8528,6 +8580,7 @@
         ..
     } = *act;
     put_u64(locals_buf);
+    put_u32(local_written);
     put_u64(spill);
     put_u32(tags);
     put_u64(call_args);
