# Shared weakref keys: first measurement screen

Before any timed samples, require all 341 VM tests, static checks, 42 targeted checks, 275 compatibility checks, and the 19-fixture preflight to pass. Compare baseline 36989234372e807ab95dc79686d9e5dd3bb51b458a9f7a1d92992357c30373a0, candidate 8899e3679267f571bc5d50167197c8a558e19c5f3278bf3e74df0a8607213284, and CPython 3.14.7. The baseline's preceding collector optimization remains independently unmeasured. The deferred candidate-position and rejected lazy-finalization runtime changes are absent.

The five retained-heap cases populate 30,000 nodes outside the work timer, with ordinary, ref, callback-ref, proxy, and callable-proxy populations. Time five full collections. The eight access cases use 100,000 iterations, including ordinary attribute and direct ref.__call__ controls. The six construction cases use five rounds on populations of 3,000 nodes: five wrapper-replacement controls release old wrappers and collect before constructing new ones, preventing callback-less ref caching; callback churn creates and releases new targets and verifies callback delivery. Their timer includes cleanup and explicit collection. It is not a pure constructor timer.

Use one warm cycle, seven measured paired cycles, and alternating variant order. The unchanged isolated-component runner verifies each group on both releases and CPython, plus candidate GIL-disabled mode, before timing. Separate frozen caches are keyed by binary hash and must remain unchanged during measurement. Keep all samples, regressions, and source hashes. Work CPU covers the same interval as work wall time; process CPU, elapsed time, and peak RSS also include setup, warmup, and shutdown.

Launch outside the tool filesystem sandbox. The load gate requires one- and five-minute load at most four on eight logical CPUs for three consecutive ten-second observations. Its launch deadline is 600 seconds. Preserve a failed launch record; it contains no benchmark samples. Once a benchmark starts, never exclude or retry its samples because of later load. Record ongoing load and raw VM/swap state before and after the screen. This is context, not proof of an idle host or unchanged memory pressure.

Assess storage reductions and hot-path behavior together with ordinary controls, constructor cleanup, and callback costs. Static allocation sites alone cannot establish elapsed time or RSS savings. Any retained candidate still requires startup and full-census comparisons. These focused cases cannot establish superiority over CPython on every workload or metric.
